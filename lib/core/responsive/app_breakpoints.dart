/// unified breakpoint constants for the whole app
/// defined once and reused everywhere , no screen should invent its own
/// width thresholds
abstract class AppBreakpoints {
  AppBreakpoints._();

  static const double mobile = 600.0;
  static const double tablet = 1024.0;
}